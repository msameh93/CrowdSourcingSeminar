<body>
	<div class="zone" id="zone">
	<h1>Hello, <%= @current_user.name %></h1>
	<img src="<%= session[:img] %>">
	<h2>Score: <%= @current_user.score %></h2>
	<div class="nav-bar">
		<ul>
  			<li><a>Home</a></li>
  			<li><a href="/game/game">Play!</a></li>
  			<li><a href="">Challenge</a></li>
  			<li><a href="">Rank</a></li>
  			<li><a>Requests</a></li>
  			<li><a href="">Leave 3arosty</a></li>
		</ul>
	</div>
	<h4>Choose a request:</h4>
		  <% if !@request.blank? %>
		  	<% @request.each do |request| %>
		  		<%= sync partial: 'request_list_row', resource: request %>
		  	<% end %>
		  	<%= sync_new partial: 'request_list_row', resource: Request.new %>
		  <% else %>
		  	<p>No requests</p>
		  <% end %>
	</div>
</body>